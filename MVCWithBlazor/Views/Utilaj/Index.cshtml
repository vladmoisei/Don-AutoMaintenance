@model IEnumerable<MVCWithBlazor.Models.UtilajModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Utilaj)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ZonaUtilaj)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Utilaj)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ZonaUtilaj)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UtilajModelID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UtilajModelID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UtilajModelID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
